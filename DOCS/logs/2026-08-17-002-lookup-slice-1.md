# Session Log - 2026-08-17-002

## Summary

Implemented Slice 1 of the Look Up landing page test-first. `/app/lookup` now resolves to a full-bleed, always-dark `LookUpPage` (hero with hand-traced horizon mark, "Look up." H1, lede, disabled App Store badge with coming-soon note, and the footer with site/contact links and the Screen Time privacy line), rendered with no site chrome. Route maps, metadata, sitemap, and the `/apps` row (Look Up first, linking internally) are wired. The build emits `build/app/lookup/index.html` with the correct title, description, and OG tags, proving the nested-path prerender.

## Files Touched

- `src/components/LookUpPage.js` (new) — page shell, `APP_STORE_URL` constant (null = disabled badge)
- `src/components/LookUpPage.test.js` (new) — hero, disabled badge, footer, route-map pair, no-chrome integration test
- `src/LookUp.css` (new) — scoped `.lookup-page` styles, always `#080808`
- `src/App.js` — `lookup` command, `/app/lookup` route, named exports for the route maps, full-bleed render branch
- `src/siteMetadata.js` — `lookup` entry
- `public/sitemap.xml` — `/app/lookup` URL
- `src/outputs/apps.js` — Look Up row first, separator adjusted
- `public/lookup/` (new) — `lookup-mark.svg` (hand-authored trace of `LaunchLogo.png`), `lookup-icon-180/192/512.png` (resized from `AppIcon.png` via sips)
- `DOCS/specs/lookup-landing-page.md` — Slice 1 marked complete; voice rule added

## Tests

- Focused: `CI=true npm test -- --watchAll=false src/components/LookUpPage.test.js` — 5/5 pass (confirmed red first on missing module)
- Full suite: 26/26 pass across 7 suites
- `npm run build` succeeds; verified `build/app/lookup/index.html` title, description, og:image, og:url
- `git diff --check` clean
- Browser QA not run (spec defers it to Slice 5)

## Decisions

- Never describe Look Up as a "wellbeing" or "wellness" app anywhere (page, metadata, `/apps` row, docs) — Apple App Review sensitivity, per James. Added as a voice rule in the spec; scrubbed the spec's Problem section, which had "digital-wellbeing app".
- Exported `commandRouteMap`/`routeCommandMap` as named exports from `App.js` so the route-resolution test can assert both directions.
- The disabled badge is a custom text lockup ("Download on the / App Store") with `aria-disabled="true"`; no Apple badge artwork fetched.
- Hero mark alt is "Look Up" (brand logo); future divider use will be decorative with empty alt.

## Follow-ups

- Slice 2: full seven-section copy deck and responsive rhythm; extend the badge test to expect two badges and two coming-soon notes.
- `lookup-og.png` is referenced by metadata but produced in Slice 4; the OG image URL 404s until then.
- Slice 4: framed screens from SwiftUI previews in `~/code/reality-interrupt` (`OnboardingPromiseView`, picker/home).
