# Backlog

This file is the single source of truth for current work status. Keep each item to one line. If an item needs detail, create a spec in `DOCS/specs/` and link to it.

<!-- Done purge rule: when Done exceeds 20 items, or at the end of a milestone, move Done items to DOCS/archive/YYYY-MM-backlog.md and clear the section. -->

## Backlog

## In Progress

## Blocked

## Done

- Updated the site contact telephone number to `+27 68 603 8834`.
- Updated `public/sitemap.xml` to include active local routes and remove the outbound sanctuary route.
- Standardized dropdown navigation labels to title case.
- Changed book cover image paths to root-relative URLs so `/little-panda/` and `/books/` work with trailing slashes in production.
- Flattened `/books` so each book appears as its own individual section instead of category groupings.
- Expanded and highlighted `Embracing Life's Journey` on `/books` with summarized copy and the corrected Amazon link.
- Expanded and highlighted `Uncharted Waters` on `/books` with summarized copy and the corrected Amazon link.
- Expanded and highlighted `The Silicon Sage` on `/books` with summarized copy and the corrected Amazon link.
- Expanded and highlighted `The Rhyming Tao` on `/books` with summarized copy and the corrected Amazon link.
- Expanded and highlighted `The Flow of Life: 365 Days with Tao` on `/books` with summarized copy and the larger book-card treatment.
- Expanded and highlighted `See · Feel · Tao` on `/books` with larger cover treatment and the updated Amazon link.
- Added the Little Panda Tao Stories page, command, catalogue links, metadata, redirect, and sitemap entry.
- Expanded the `Little Panda Learns the Tao` description on the dedicated Little Panda page.
- Updated the Little Panda pages and metadata to use the higher-resolution `*-book.webp` cover filenames.
- Scaffolded the canonical project memory structure from `startup-template.md.md`.
